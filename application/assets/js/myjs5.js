$(document).ready(function(){
    
    var person ={
        // para o id (fullname,age)
        fullname: "Alda",
        age:22
    }

    $('#fullname').val(person.fullname);
    $('#age').val(person.age);
});


